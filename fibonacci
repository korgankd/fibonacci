var count;

count = function (end) {
	var first = 1;
	var second = 1;
	var output = "";
	for (var i = 0; second < end && i < end; i++) {
		output += first + " ";
		first = second + first;
		output += second + " ";
		second = second + first;
	}
	return output;
}

module.exports = {
	count: count,
};